import { ResultClient } from "./ResultClient";

export const metadata = {
  title: "Your result — Is this a scam?",
  description:
    "Your scam-risk read, the reasons behind it, and what to do next.",
};

export default function ResultPage() {
  return <ResultClient />;
}
